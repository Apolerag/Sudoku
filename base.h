#ifndef BASE_H
#define BASE_H

#include<iostream>
#include <vector>
#include <set>


typedef enum{
    noError = 0,
    Vide = 1,
    Nochange = 1
} Error;

typedef enum {
    vide,
    test,
    valide
} Etat;

#endif // BASE_H
